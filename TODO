- gtk_window_set_role() for each toplevel window, important for restoring
  with session management!

- Write session management support.

- Use version number in Terminal D-BUS interface, starting with version 0.2
  to provide some means of backward compatibility?

- Add a faq entry about how to customize the Terminal.ui.
